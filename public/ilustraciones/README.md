# Ilustraciones de /primeros-pasos

Origen: [unDraw](https://undraw.co) (licencia MIT — uso comercial libre, sin
atribución obligatoria). Recoloreadas del morado original (`#6c63ff`) al
verde de marca (`#5A6142`) editando directamente el atributo `fill` de cada
SVG — sin depender del selector de color del propio sitio, poco fiable por
clic.

| Archivo | Slug original en unDraw |
|---|---|
| `hero.svg` | `goals` |
| `configuracion.svg` | `online-calendar` |
| `pagos.svg` | `online-payments` |
| `automatizaciones.svg` | `reminders` |
| `equipo.svg` | `meet-the-team` |
| `portal.svg` | `mobile-app` |
| `completado.svg` | `project-completed` |

Usadas en `app/(dashboard)/primeros-pasos/page.tsx`.
