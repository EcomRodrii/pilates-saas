// Color de acento de Tentare Network, usado por SeccionRegistro.tsx —
// separado de components/landing/data.ts (que es de Tentare Manager) porque
// es un producto distinto con su propia narrativa.
export const NW = '#4F8A5B';
