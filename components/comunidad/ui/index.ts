// Las tres piezas que `post-card-portal.tsx` usaba de `components/portal/ui`.
// Se copian aquí al borrar el portal de la alumna: son de Comunidad, que
// sigue viva, y no tenían por qué depender de una carpeta que ya no existe.
export { AforoIndicator } from './AforoIndicator';
export { Badge } from './Badge';
export { Button } from './Button';
