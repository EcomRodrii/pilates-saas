// El aviso de «página oculta», en un solo sitio para las dos caras que lo
// enseñan: la pantalla de /reservar y la app (components/publico/pagina-oculta.tsx)
// y el calendario incrustado (app/widget-bundle/main.tsx). Sin imports: el
// widget se compila aparte para el navegador.
export const AVISO_PAGINA_OCULTA = 'Estamos preparando esta página. Vuelve dentro de poco.';
